# TaPaaS v0.3 evidence log

| Date | Task | Input artefacts used | Output produced | What worked | What failed or remains unknown | Accessibility issues | Engineering issues | Next action |
|---|---|---|---|---|---|---|---|---|
| 2026-05-19 | Parse TaPaaS component library | `Components - TaPaaS Design Library (Copy).fig` | Source inventory and component candidates | Local parser exposed page names, node IDs, text content, symbols and instances | Live Figma MCP still needed for final constraints and variants | Accessibility annotations exist but are incomplete in several components | `.fig` parser is reverse-engineered, so treat output as draft | Confirm high-priority components in live Figma when possible |
| 2026-05-19 | Parse TaPaaS template library | `Templates - TaPaaS Design Library.fig` | Page template registry | Extracted Privacy, Search/Input, Declaration, Review, Confirmation, Receipt, modal and error-page templates | Some templates are design-only for this pack | Modal and error-page behaviours need deeper review | Coded skeleton intentionally excludes modal/session/error routing | Use templates to guide repeatable skeleton only |
| 2026-05-19 | Register first 12 components | Component library text and node IDs | `docs/tapaas/01-component-registry.md` | Components now have classification and maturity labels | GEL alignment is not fully verified for all components | Card radio buttons require accessibility review | Only 5 composites are coded in this release | Pair with engineering before promoting any component |
| 2026-05-19 | Add TaPaaS steering | Existing GEL steering plus TaPaaS evidence | `.kiro/steering/10-14-*` | Kiro now has explicit rules for skeletons, selection, evidence, safety and prompts | Kiro behaviour still needs trial feedback | Steering blocks compliance overclaims | Steering relies on engineers following maturity labels | Gather participant feedback after first use |
| 2026-05-19 | Add coded preview composites | TaPaaS registry and GEL-shaped preview components | `src/tapaas-preview/` and skeleton flow | Built 5 small composites without changing `src/gel.ts` | Components are preview-only and need review | Focus, semantics and headings need manual browser smoke check | No backend, payment, identity or policy logic included | Run build and browser checks |
| 2026-05-19 | Apply accessibility smoke-test fixes | Browser and Puppeteer accessibility smoke-test reports | Added `aria-live` to step indicator, `role="status"` to confirmation header, footer heading level fix, and larger preview control touch targets | Addressed the small fixes identified by trial testing | Assistive technology behaviour still needs VoiceOver/NVDA review | No formal WCAG compliance claim | Changes are preview-layer only and require continued manual testing | Re-run Kiro accessibility smoke test |
| 2026-05-19 | Implement trial permit skeleton | Implementation plan, existing vehicle skeleton pattern, TaPaaS component registry, page template registry | `src/TrialPermitSkeleton.tsx`, updated `src/App.tsx` with skeleton switcher | Reused all 5 TaPaaS preview composites and 8 GEL preview components without new component creation. Input page uses Field+Input for name and RadioButtonList for permit type. Build passed and browser smoke test passed 22/22. | Privacy, legal, fee, timeframe and reference content all need owner confirmation. No backend, payment, identity or eligibility logic. | Step indicator uses aria-live="polite". Error summary focus management reused from vehicle skeleton. Needs VoiceOver/NVDA review. | No new components created. Pattern follows build-tested vehicle skeleton structure. | Use as repeatability evidence and continue with selected real candidate |
| 2026-05-19 | Implement community event permit (8-step) | Expansion plan, existing skeleton patterns, component registry, page template registry | `src/CommunityEventPermitSkeleton.tsx`, `Textarea` added to gel-preview, updated App.tsx with 3-skeleton switcher, updated docs and steering | 8-step flow works with existing components plus one Textarea addition. Multi-page form input pattern (applicant, contact, event, supporting) scales cleanly. ReviewInfoCard handles 4 sections. Date input uses 3-field pattern. Address uses Field+Input+Select composition. | All privacy, legal, fee, timeframe and reference content are placeholders. No backend, payment, identity or eligibility logic. File upload excluded. | aria-live step indicator works for 8 steps. Longer flows need VoiceOver testing for orientation. No formal WCAG compliance claim. | Textarea is the only new GEL preview component. No new TaPaaS composites. Pattern proves pack scales beyond 5 steps. | Run build and browser smoke test |
| 2026-05-19 | UX and accessibility hardening from stress testing | Deployed stress-test findings across 3 skeletons | Step focus/scroll, confirmation focus, exit notice focus, switcher aria-pressed, chrome link fix, review heading hierarchy, validation hardening, autocomplete attributes, source link fix | All fixes applied without new components or visual changes | VoiceOver/NVDA testing still needed. Longer flows may need a confirmed TaPaaS/GEL progress indicator pattern (documented, not built). | Focus management improved. No WCAG compliance claim. | No new dependencies. Pattern remains preview-only. | Assistive technology testing with VoiceOver and NVDA |
| 2026-05-19 | Deployed retest - 70/70 smoke checks passed | Deployed app at https://leokessel-lgtm.github.io/tapaas-kiro-trial/, browser and accessibility-oriented smoke checks across all 3 skeletons | 70/70 pass across switcher, validation, review, confirmation, mobile smoke checks and accessibility-oriented checks. Vehicle shows `TAPAAS-000000`, Trial permit shows `PERMIT-000000`, and Community event permit shows `EVENT-000000`. | Skeleton switcher works with `aria-pressed`. Community event permit completes 8 steps. Error summaries receive keyboard focus, links target the correct fields, step transitions scroll to top and focus headings, confirmation pages focus `role="status"` headers, source inventory link resolves, autocomplete attributes are present, and no rendered compliance claims were detected. Pattern remains repeatable across 3 transaction skeletons without new TaPaaS composites. | VoiceOver/NVDA testing not performed. Longer flows may benefit from a confirmed TaPaaS/GEL visual progress indicator. `role="group"` on error summary needs confirmation against the actual GEL ErrorSummary pattern. Dual H1 on confirmation pages remains a known observation. | GEL error summary uses `role="group"` with `aria-labelledby` and `tabIndex=-1` for programmatic focus. All visible transaction form controls have accessible names. Checkbox and radio selected states use brand colour `#002664`. No WCAG compliance, accessibility approval or production readiness claim. | No new dependencies. No new TaPaaS composites. `Textarea` is the only GEL preview addition. Build passes with `tsc + vite`. Steering file 10 includes progress indicator guidance for 6+ step flows. | Share deployed URL with trial participants, gather feedback on focus behaviour/content clarity/reusability, and schedule VoiceOver/NVDA testing if the trial proceeds to accessibility review. |
| 2026-05-19 | Repeatability test — Community stall permit (4th skeleton) | Existing steering files 10–14, CommunityEventPermitSkeleton pattern, GEL preview components, TaPaaS preview composites | `src/CommunityStallPermitSkeleton.tsx`, updated `src/App.tsx` with 4-skeleton switcher. Build passed (43 modules). Browser smoke test 20/20. | No new components required. Stall permit skeleton built entirely from existing GEL preview (Heading, Field, Input, Select, Textarea, Checkbox, RadioButtonList, ErrorSummary, InPageAlert, TextLink) and TaPaaS preview (TransactionCtaGroup, ReviewInfoCard, ReviewFeesCard, ConfirmationHeader, TransactionSummaryCard). Pattern copied from event permit with stall-specific fields. Validation, focus management, scroll, aria-live, role="status", autocomplete all carried over. | All placeholder content needs owner confirmation. No backend, payment, identity or eligibility logic. VoiceOver/NVDA testing not performed. | Focus management, aria-live step indicator, role="status" confirmation, error summary role="group" all reused without modification. No formal WCAG compliance claim. | No new dependencies. No new GEL or TaPaaS composites. 4 skeletons now share the same component set. Build passes. Pattern is confirmed repeatable. | Repeatability confirmed: a new 8-step transaction skeleton can be built from the existing v0.3 component set in a single pass without new components. The pack supports 5-step and 8-step flows from the same adapter and composite layers. |
| 2026-05-19 | Complexity push — 3 new preview components + 9-step accessible market permit | Existing component registry, steering files 10–14, Details card single `2413:787` Figma evidence, conditional declaration `27:56000` pattern | `ProgressStepper` (GEL preview), `DetailsCard` (TaPaaS preview), `ConditionalQuestionPanel` (TaPaaS preview), `src/AccessibleMarketPermitSkeleton.tsx`, updated App.tsx with 5-skeleton switcher, updated component registry | 9-step flow works with 3 new components. ProgressStepper uses aria-current="step" and wraps on mobile. DetailsCard provides read-only context on later pages. ConditionalQuestionPanel shows/hides fields based on radio answer with conditional validation. Browser smoke test 17/17. Build passes (44 modules). | VoiceOver/NVDA testing not performed. ProgressStepper step-click navigation intentionally disabled (future enhancement). ConditionalQuestionPanel focus management on show/hide needs AT verification. DetailsCard heading level needs confirmation against TaPaaS Figma source. | ProgressStepper uses semantic ol with aria-current. ConditionalQuestionPanel uses fieldset/legend with conditional content immediately after radio group. Error summary links target controlling question or conditional field. No formal WCAG compliance claim. | 3 new components added (1 GEL-aligned, 2 TaPaaS-specific). Total component count: GEL preview 15, TaPaaS preview 8. Build passes. No new dependencies. This is the most complex skeleton in the pack (9 steps, conditional logic, progress stepper, details card). | More complex than the stall permit repeatability test. Introduces conditional branching, visual progress indication, and contextual summary cards. Proves the pack can handle interactive conditional patterns without backend logic. |
