# Transaction Coverage Matrix

## Purpose

This matrix keeps MPS transaction assembly connected to source-backed preview artefacts, Storybook review stories, transaction skeleton usage, tests and evidence.

It is trial guidance only. It does not claim production readiness, WCAG compliance, GEL approval, TaPaaS approval, legal approval, privacy approval or policy approval.

## Clara/TaPaaS Rule Status

This matrix was created for MPS source-frame coverage before Clara/TaPaaS rules were promoted into the trial steering layer. Status values such as `aligned` mean source-frame, Storybook, skeleton, test and evidence connections are present. They do not mean Clara/TaPaaS-rule alignment.

For current Clara/TaPaaS transaction rules, start with:

- `docs/tapaas/12-clara-tapaas-transaction-rules-v01.md`
- `docs/tapaas/designer-package/README.md`

As of Slice 7A, only Trial Permit and Accessible Market Permit are Clara-aligned runtime examples. MPS remains a legacy/pre-Clara evidence surface with source-frame-aligned areas, but it is not yet Clara/TaPaaS-rule aligned as a transaction.

Status values:

- `aligned`: source-backed preview, Storybook review surface, transaction usage, tests and evidence are connected. This means source-frame aligned, not Clara/TaPaaS-rule aligned.
- `partial`: some connection exists, but one or more surfaces are missing or weak.
- `skeleton-only`: implemented mainly in the transaction skeleton without a source-backed preview frame/story.
- `story-only`: available in Storybook but not used in the transaction skeleton.
- `review-gated`: bounded preview exists, but owner, Figma, backend, content, policy/privacy or accessibility evidence is still needed.
- `missing`: no useful preview/story/skeleton coverage yet.
- `out-of-scope`: deliberately excluded from the current preview transaction.

## MPS Transaction Coverage

| Transaction stage | Preview artefact | Storybook review story | Transaction skeleton usage | Test coverage | Evidence/map status | Status | Unknowns / review gate |
|---|---|---|---|---|---|---|---|
| privacy/start | `PrivacyCardPreview`; `ExitModal`; `InPageAlert`, `Checkbox`, `TransactionCtaGroup`; mock account/context support uses `DetailsCard` and `InteractiveDetailsCard` | `Privacy Card Pattern`; `Transaction Action Areas`; `Exit Modal`; `Details Card`; `Interactive Details Card`; `MPS End-to-End Transaction Assembly v1`; no isolated MPS privacy/start fidelity story | `PrivacyStep` and `AccountStep` in `MobilityParkingPermitSkeleton.tsx`; exit modal is available from every MPS step; privacy card, details card and interactive details card remain catalogue-level review patterns | MPS flow test blocks privacy step and exercises account acknowledgement; preview tests cover privacy card sections, acknowledgement/error state, details-card read-only rows/action, interactive details actions and exit modal labelled dialog/actions/Escape/tab-wrap smoke | Privacy card is coded-preview and mapped to Privacy step `3395:41359`; exit modal mapped to template `4677:1042`; details card mapped to node `2413:787`; interactive details card mapped to node `2958:2499`; relationship map says wording, identity and exit behaviour must be owner-confirmed | partial | Final privacy collection notice, account/identity source parity, identity behaviour, action routing, exit/save/session behaviour, privacy approval, legal/policy wording and assistive-technology behaviour remain review-gated. |
| application type / branching | `RadioButtonCards` for new/renew/replace; GEL `Field`, `Input`, `Select` for conditional permit/replacement inputs | `MPS End-to-End Transaction Assembly v1`; `RadioButtonCardStates`; acceptance manifest tracks the radio-card state story and transaction assembly story | `ApplicationTypeStep` uses `RadioButtonCards` and conditional inputs | MPS flow test selects new application; branch tests cover new, renewal and replacement paths, replacement reason review carry-through and no lookup/backend button; preview tests cover radio-card semantics and state fallback | Radio-button cards mapped to source node `31:63988`; relationship map marks Figma status `CONCEPT`; acceptance manifest now tracks application branching | partial | Exact MPS branch frame fidelity, selected/hover/focus parity, final branch copy and accessibility behaviour need review. |
| applicant details / manual address | `MpsApplicantDetailsFramePreview` | `MPS End-to-End Transaction Assembly v1`; `MPS Applicant Details Frames - Figma Fidelity`; transaction assembly story includes the frame | `ApplicantStep` now uses `MpsApplicantDetailsFramePreview` through a thin adapter | MPS test covers search-address/default state, manual-address state, navigation and mocked review carry-through; preview tests cover both frame variants | Registry and relationship map include MPS applicant details frames `0:17387`, `0:17405`; evidence log records extraction, mobile overflow fix and transaction alignment | aligned | Real address lookup, address result/no-result states, final validation/error-summary behaviour, customer-record behaviour, final content, mobile pixel parity and assistive-technology behaviour remain review-gated. |
| representative/contact details | `ConditionalQuestionPanel`, `RepeatableGroup`, GEL `Field`/`Input`; applicant contact details in `2.A - Personal details` `0:17387` and `2.B - Personal details - Manual address` `0:17405` may be used only as structural reference | `Conditional Question Panel`; `MPS End-to-End Transaction Assembly v1`; no isolated representative/contact fidelity story | `RepresentativeStep` in the MPS skeleton | MPS flow test covers no-representative path only; conditional panel tests cover reveal behaviour and error association | No standalone MPS representative/contact details frame was found in `MPS Final`. Relationship map notes representative and repeatable authorised contacts as an intended MPS simulation area, but there is no dedicated source-backed frame pattern. Conditional panel coverage is generic preview support only. | skeleton-only | Representative, nominee, authority, relationship, consent, identity and privacy rules are not confirmed. Validation rules, repeatable-contact variants, owner-approved labels/content and accessibility behaviour remain review-gated. |
| evidence/medical | `EvidenceChecklistCard`, `MpsMedicalEvidenceStatusPreview`, `AssessmentSummaryPanel`, GEL radio lists, GEL accordion | `MPS End-to-End Transaction Assembly v1`; `Mps Evidence And Assessment`; `Evidence Checklist Card`; `Assessment Summary Panel`; `MPS Medical Evidence Status - Figma Evidence` | `MedicalEvidenceStep` now composes `MpsMedicalEvidenceStatusPreview` through a thin preview-only adapter; review evidence status and assessment summary remain mock-only | MPS flow test covers medical certificate and mock uploaded path; skeleton tests cover certificate uploaded, report uploaded, provide-later/manual-review routing and absence of upload/remove controls; preview tests cover evidence checklist status rows, assessment summary rows and certificate/report required and provided medical evidence status states without upload/remove controls | Evidence checklist mapped to MPS medical frames and GEL file-upload/status-label evidence; isolated evidence checklist and assessment summary acceptance entries added; `MpsMedicalEvidenceStatusPreview` extracts certificate frames `4.A`/`4.Aa` nodes `0:17316`/`0:17333` and report frames `4.B`/`4.Ba` nodes `0:17357`/`0:17370`; boundary nodes `0:17327`, `0:17344`, `0:17351`, `0:17369`, `0:17381`, `0:17384`, `0:17385`, `0:17386`; relationship map explicitly excludes real upload/storage/validation | partial | Source upload-state, heading, file-limit and report-section wording inconsistencies remain review-gated. Exact medical evidence rules, upload states, file validation, storage, privacy/security handling, medical content and assistive-technology behaviour remain review-gated. |
| concession | GEL `Select`, GEL radio lists, `AssessmentSummaryPanel`, `BackendErrorExamplePage` for mock hard stops | `MPS End-to-End Transaction Assembly v1`; `Assessment Summary Panel`; covered indirectly in selected maturity/backend error examples and `Mps Evidence And Assessment`; no isolated concession frame story | `ConcessionStep`, review status, outcome hard-stop branch | MPS flow test covers no-concession happy path; backend examples covered in preview tests; assessment summary preview test covers mock status rows only | Relationship map identifies concession frames and backend/business error patterns but says real backend validation requires source-confirmed rules; assessment summary is now isolated for mock status review only | partial | Centrelink/DVA invalid, duplicate and mismatch frame parity; backend validation rules; recovery routes; content and policy review remain unknown. |
| declaration | `DeclarationReview` exists; MPS skeleton declaration step uses GEL checkbox plus warning; `MpsReviewFramePreview` includes checked declaration preview | `MPS End-to-End Transaction Assembly v1`; `Declaration Review`; `Selected Maturity Components`; `MPS Review Frame - Bounded Source Review` | `DeclarationStep` uses skeleton-only checkbox wording; review page uses `MpsReviewFramePreview` declaration area | MPS flow test accepts declaration; preview tests cover declaration review paired variants and MPS review declaration rendering | Declaration review and MPS review frame are registered/mapped; isolated declaration review acceptance added; final declaration/legal wording is explicitly unresolved | partial | Final legal/privacy/declaration wording, checkbox validation semantics, whether declaration playback/card/accordion is correct, and policy/legal approval remain review-gated. |
| review | `MpsReviewFramePreview`, `ReviewInfoCard`, `LegalInfoAccordion`, `ReviewFeesCard`, `EvidenceChecklistCard`, `AssessmentSummaryPanel`, `InteractiveDetailsCard` | `MPS End-to-End Transaction Assembly v1`; `MPS Review Frame - Bounded Source Review`; `Review and Confirmation - Transaction Assembly`; `Review Info Card`; `Evidence Checklist Card`; `Assessment Summary Panel`; `Interactive Details Card`; `Legal Info Accordion`; `Declaration Review`; selected maturity story for supporting components | `ReviewStep` uses `MpsReviewFramePreview` and supporting preview components | MPS flow test reaches review and submits; preview tests cover MPS review frame, review info card, evidence checklist, assessment summary, interactive details actions, legal accordion placeholders, declaration review variants and supporting components | Registry and relationship map include MPS review frame `0:33185`, review info card `18:4448`, legal accordion, review fees, evidence checklist, assessment summary, interactive details card and declaration review | aligned, bounded | Edit routing, exact source text, legal/privacy content, action routing, evidence handling, assessment status wording, payment/fee rules, mobile pixel parity, full-frame chrome/footer parity and assistive-technology behaviour remain review-gated. |
| confirmation | `MpsConfirmationFramePreview`, `ConfirmationHeader`, `TransactionSummaryCard`, `NextStepsCardPreview` internally, `TransactionCtaGroup` | `MPS End-to-End Transaction Assembly v1`; `MPS Confirmation Frame - Bounded Source Review`; `Review and Confirmation - Transaction Assembly`; `Confirmation Header`; `Next Steps Card`; `Transaction Summary Card` | `OutcomeStep` uses `MpsConfirmationFramePreview` for submitted/manual-review outcomes; confirmation header remains covered as catalogue-level support | MPS flow test checks successful confirmation reference; preview tests cover confirmation header status semantics, confirmation frame, transaction summary and next-steps patterns | Registry and relationship map include MPS confirmation frame `0:33222`, confirmation header `9:10494`, transaction summary card `10:1861` and next steps card `11:4848` | aligned, patched and bounded | Real reference format, notification timing, feedback behaviour, related transaction routing, receipt/outcome content, mobile pixel parity, full-frame chrome/footer parity and assistive-technology behaviour remain review-gated. |
| technical hard-stop/error-shell preview coverage | `SystemErrorPage` | `System Error Page` | Catalogue-level review only; not wired into the transaction skeleton | Preview tests cover system error mock action callbacks only | System error page mapped to source node `17628:2069`; real retry routing, start-over routing, logout/session handling, operational ownership and system error taxonomy remain out of scope | review-gated | Real system error taxonomy, retry/start-again/logout behaviour, session handling, operational ownership, backend behaviour and assistive-technology behaviour remain unknown. |
| recoverable/business errors | `BackendErrorExamplePage`, `backendErrorExamples`, `BusinessErrorPage` | `MPS End-to-End Transaction Assembly v1`; `Business Error Page`; `Selected Maturity Components`; backend/business error example story | `OutcomeStep` uses backend examples for mock payment and concession hard stops | Preview tests cover isolated business error page rendering, backend error rendering and MPS mock payment-failure hard stop without submitted reference | Business error page mapped to source node `8931:31271`; backend error examples mapped to source node `31:73426`; relationship map says real use requires source-confirmed business rules | review-gated | Real backend error routing, recovery rules, source-confirmed codes, retry/start-again behaviour, analytics/storage and operational ownership remain unknown. |

## Current Strongest Connections

- Applicant details/manual address is the strongest early/middle source-frame-aligned stage because it now connects source-backed frame extraction, isolated Storybook review, transaction skeleton usage, tests and evidence.
- Review is strongly source-frame connected through `MpsReviewFramePreview`, supporting preview components, Storybook fidelity and transaction usage.
- Confirmation is strongly source-frame connected through `MpsConfirmationFramePreview`, next-steps preview content, Storybook fidelity and outcome usage.

## Current Weakest Connections

- Privacy/start remains partial because the privacy card is now coded-preview, but privacy wording, legal/policy wording and identity/account behaviour are still placeholders or owner-gated.
- Representative/contact details remains skeleton-only and should not be treated as source-backed. Applicant contact details in `2.A` and `2.B` can inform structure only; they do not confirm representative, nominee, authority, relationship, consent, identity or privacy rules.
- Concession and recoverable/business errors remain review-gated because real backend validation and recovery rules are not evidenced.

## Next Alignment Action

The next highest-value transaction alignment action is to keep representative/contact details documented as a source gap unless a dedicated MPS representative/contact frame is found or owner evidence is supplied. If neither exists, continue treating the skeleton implementation as mock-only and use applicant contact details only as structural reference.
